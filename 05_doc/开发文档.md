# 校园生活服务平台 — 开发文档

## 1. 开发环境

### 1.1 环境要求

| 工具 | 版本 | 用途 |
|------|------|------|
| .NET SDK | 10.0 | 后端编译运行 |
| Node.js | 18+ LTS | 前端编译运行 |
| MySQL | 8.0 | 数据库 |
| Redis | 7.x | 缓存服务 |
| RabbitMQ | 3.x | 消息队列 |
| Docker | 24+ | 容器化部署 |
| Visual Studio 2022 / VS Code | 最新 | IDE |
| Android Studio | 最新 | Android APK打包 |

### 1.2 开发工具推荐

- **后端**: Visual Studio 2022 / JetBrains Rider
- **前端管理端**: VS Code + Volar + ESLint + Prettier
- **移动端**: HBuilderX (UniApp官方IDE)
- **数据库**: Navicat / DBeaver / DataGrip
- **API测试**: Postman / Swagger

## 2. 快速开始

### 2.1 基础设施启动

```bash
cd e:\kmccXM\03_server
docker-compose up -d
# 启动 MySQL(:3306) + Redis(:6379) + RabbitMQ(:5672/:15672)
```

### 2.2 数据库初始化

```bash
# Docker Compose 自动执行 init_database.sql
# 或手动导入:
mysql -u root -proot123 < e:\kmccXM\04_sql\sql\init_database.sql
mysql -u root -proot123 < e:\kmccXM\04_sql\sql\init_admin_user.sql
```

管理员初始账号: `admin` / 密码: `admin123`

### 2.3 后端启动

```bash
cd e:\kmccXM\03_server

# 一键启动全部服务
.\start-all-services.ps1

# 或单独启动
dotnet run --project Services\User.Service
dotnet run --project ApiGateway
```

### 2.4 前端启动

```bash
# 管理端
cd e:\kmccXM\02_admin && npm install && npm run dev
# 访问 http://localhost:3000

# 商家端
cd e:\kmccXM\02_merchant && npm install && npm run dev
# 访问 http://localhost:3001

# 移动端
cd e:\kmccXM\01_uniapp\uniapp && npm install && npm run dev
# 访问 http://localhost:8080
```

## 3. 后端开发指南

### 3.1 项目引用关系

```
Campus.Common (无项目引用)
    ↑
Campus.Infrastructure (引用 Campus.Common)
    ↑
所有微服务 + ApiGateway (引用 Campus.Common + Campus.Infrastructure)
```

### 3.2 新增微服务模板

```csharp
// Program.cs
var builder = WebApplication.CreateBuilder(args);
builder.Services.AddControllers();

// JWT
var jwtKey = builder.Configuration["Jwt:SecretKey"] ?? Campus.Common.Constants.JwtSecretKey;
builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme).AddJwtBearer(options => { ... });

// EF Core
builder.Services.AddDbContext<YourDbContext>(o => 
    o.UseMySql(connStr, ServerVersion.AutoDetect(connStr)));

// Redis
builder.Services.AddCampusRedis(builder.Configuration.GetConnectionString("Redis") ?? Campus.Common.Constants.RedisConnectionString);

// CORS
builder.Services.AddCors(o => o.AddDefaultPolicy(p => p.AllowAnyOrigin().AllowAnyMethod().AllowAnyHeader()));

// App
var app = builder.Build();
app.UseCors();
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();
app.Urls.Add("http://0.0.0.0:<YOUR_PORT>");
app.Run();
```

### 3.3 Controller 规范

```csharp
[ApiController]
[Route("api/v1/[controller]")]
public class YourController : ControllerBase
{
    private readonly YourDbContext _db;
    public YourController(YourDbContext db) { _db = db; }

    [HttpGet("list")]
    public async Task<IActionResult> List([FromQuery] PageRequest page)
    {
        var query = _db.YourEntities.AsQueryable();
        var total = await query.CountAsync();
        var list = await query.Skip((page.Page - 1) * page.PageSize).Take(page.PageSize).ToListAsync();
        return Ok(ApiResponse<PageResult<YourEntity>>.Success(
            PageResult<YourEntity>.Of(list, total, page.Page, page.PageSize)));
    }

    [HttpGet("{id}")]
    public async Task<IActionResult> Detail(long id)
    {
        var entity = await _db.YourEntities.FindAsync(id);
        if (entity == null) return Ok(ApiResponse.Error(404, "未找到"));
        return Ok(ApiResponse<YourEntity>.Success(entity));
    }

    [HttpPost]
    public async Task<IActionResult> Create([FromBody] YourRequest req) { ... }

    [HttpPut("{id}")]
    public async Task<IActionResult> Update(long id, [FromBody] YourRequest req) { ... }
}
```

### 3.4 全局 Usings

每个服务包含 `GlobalUsings.cs`:

```csharp
global using Campus.Common;
global using Campus.Infrastructure;
global using Campus.Common.Security;
global using Microsoft.AspNetCore.Mvc;
global using Microsoft.EntityFrameworkCore;
```

### 3.5 用户认证

```csharp
// 需要认证的接口添加 [Authorize] 特性
[HttpPost]
[Authorize]
public async Task<IActionResult> ProtectedAction() { ... }

// 获取当前用户ID
var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
```

## 4. 前端开发指南

### 4.1 管理端/商家端 (Vue 3 + Element Plus)

#### 项目结构模式
```
src/
├── api/index.ts       # API接口定义
├── router/index.ts    # 路由配置
├── stores/user.ts     # Pinia状态管理  
├── utils/request.ts   # Axios请求封装
└── views/
    └── ModuleName/
        └── index.vue  # 模块页面(SFC单文件组件)
```

#### API模块示例
```typescript
// api/index.ts
import request from '@/utils/request'

export const getList = (params: any) => request.get('/v1/entity/list', { params })
export const getDetail = (id: number) => request.get(`/v1/entity/${id}`)
export const create = (data: any) => request.post('/v1/entity', data)
export const update = (id: number, data: any) => request.put(`/v1/entity/${id}`, data)
export const remove = (id: number) => request.delete(`/v1/entity/${id}`)
```

#### Vue组件模式
```vue
<template>
  <el-card>
    <template #header>
      <div class="card-header">
        <span>标题</span>
        <el-button type="primary" size="small" @click="handleCreate">新增</el-button>
      </div>
    </template>
    <el-table :data="list" border v-loading="loading">
      <el-table-column prop="id" label="ID" width="80" />
      <el-table-column label="操作" width="200">
        <template #default="{row}">
          <el-button size="small" @click="handleEdit(row)">编辑</el-button>
          <el-button size="small" type="danger" @click="handleDelete(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
    <div class="pagination">
      <el-pagination v-model:current-page="page" :total="total" :page-size="20"
        layout="total,prev,pager,next" @current-change="fetchData" />
    </div>
  </el-card>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getList } from '@/api'
import { ElMessage } from 'element-plus'

const list = ref<any[]>([])
const page = ref(1)
const total = ref(0)
const loading = ref(false)

const fetchData = async () => {
  loading.value = true
  try {
    const res = await getList({ page: page.value, pageSize: 20 })
    list.value = res.data?.list || []
    total.value = res.data?.total || 0
  } catch (e) { ElMessage.error('加载失败') }
  loading.value = false
}

onMounted(fetchData)
</script>

<style scoped lang="scss">
.card-header { display: flex; justify-content: space-between; align-items: center; }
.pagination { margin-top: 16px; text-align: right; }
</style>
```

### 4.2 移动端 (UniApp Vue 3)

#### 项目结构
```
uniapp/src/
├── api/index.ts       # 统一API层 (41个接口)
├── utils/request.ts   # uni.request封装
├── stores/user.ts     # Pinia用户状态
├── pages.json         # 路由+TabBar配置
├── manifest.json      # 应用配置
├── pages/             # 35个页面
│   ├── home/          # 首页
│   ├── auth/          # 登录/注册
│   ├── merchant/      # 商家列表/详情
│   ├── product/       # 商品详情
│   ├── order/         # 下单/订单列表/详情/评价
│   ├── user/          # 个人中心/信息编辑
│   ├── address/       # 地址管理
│   ├── wallet/        # 钱包/流水/提现
│   ├── coupon/        # 优惠券中心/我的
│   ├── post/          # 帖子列表/详情/发布
│   ├── errand/        # 跑腿列表/详情/发布
│   ├── secondhand/    # 二手列表/详情/发布
│   ├── lostfound/     # 失物列表/详情/发布
│   ├── delivery/      # 骑手中心/申请
│   └── feedback/      # 反馈列表/提交
│
└── static/tab/        # TabBar图标
```

#### UniApp页面模式
```vue
<template>
  <view class="page">
    <view class="list">
      <view class="item" v-for="item in list" :key="item.id" @click="goDetail(item.id)">
        <!-- item content -->
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { onShow } from '@dcloudio/uni-app'
import { getList } from '@/api'

const list = ref<any[]>([])

const fetchData = async () => {
  const res = await getList({ page: 1, pageSize: 20 })
  list.value = (res.data as any)?.data?.list || []
}

onShow(fetchData)

const goDetail = (id: number) => uni.navigateTo({ url: `/pages/xxx/detail?id=${id}` })
</script>

<style scoped lang="scss">
.page { padding: 20rpx; }
</style>
```

## 5. 数据库管理

### 5.1 Migration

EF Core Migration 操作:

```bash
# 在服务目录下
cd Services/User.Service
dotnet ef migrations add Init
dotnet ef database update
```

### 5.2 表与服务的映射关系

| 表名 | 所属服务 | 访问方式 |
|------|---------|---------|
| school, campus, delivery_zone | Campus.Service | 直接访问 |
| user, role, user_role, user_address, feedback | User.Service | 直接访问 |
| merchant, category, product, product_spec | Merchant.Service | 直接访问 |
| delivery_order, order_item, errand_order, order_comment | Order.Service | 直接访问 |
| rider | Delivery.Service | 直接访问 |
| post_category, post, post_like, post_comment | Social.Service | 直接访问 |
| second_goods, lost_found, advertisement | Social.Service | 直接访问 |
| user_wallet, wallet_log, withdraw | Wallet.Service | 直接访问 |
| coupon, user_coupon | Coupon.Service | 直接访问 |

## 6. API接口汇总

### 6.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/send-code | 发送短信验证码 |
| POST | /api/v1/auth/password-login | 手机号密码登录 |

### 6.2 用户接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/user/info | 获取个人信息 |
| PUT | /api/v1/user/info | 修改个人信息 |
| GET | /api/v1/user/list | 用户列表（管理员） |
| PUT | /api/v1/user/{id} | 修改用户 |
| DELETE | /api/v1/user/{id} | 删除用户 |
| GET | /api/v1/user/stats | 用户统计 |

### 6.3 订单接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/order/create | 创建订单 |
| GET | /api/v1/order/my-orders | 我的订单 |
| GET | /api/v1/order/{id} | 订单详情 |
| POST | /api/v1/order/{id}/cancel | 取消订单 |
| POST | /api/v1/order/{id}/accept | 商家接单 |
| POST | /api/v1/order/{id}/complete | 完成订单 |
| GET | /api/v1/order/list | 订单管理列表 |
| GET | /api/v1/order/statistics | 订单统计 |
| GET | /api/v1/order/merchant-stats | 商家订单统计 |

### 6.4 跑腿接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/order/errand/create | 发布跑腿 |
| GET | /api/v1/order/errand/list | 跑腿列表 |
| GET | /api/v1/order/errand/{id} | 跑腿详情 |

### 6.5 社交接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/social/post/create | 发布帖子 |
| GET | /api/v1/social/post/list | 帖子列表 |
| GET | /api/v1/social/post/detail/{id} | 帖子详情 |
| POST | /api/v1/social/post/{id}/like | 点赞帖子 |
| POST | /api/v1/social/secondhand/create | 发布二手 |
| GET | /api/v1/social/secondhand/list | 二手列表 |
| GET | /api/v1/social/secondhand/detail/{id} | 二手详情 |
| POST | /api/v1/social/lostandfound/create | 发布失物 |
| GET | /api/v1/social/lostandfound/list | 失物列表 |
| GET | /api/v1/social/lostandfound/detail/{id} | 失物详情 |
| GET | /api/v1/social/advertisement/position/{position} | Banner广告 |

完整API共 **41个** 接口覆盖 8 个微服务。

## 7. 编译部署

### 7.1 后端编译

```bash
# 全栈编译
dotnet build Campus.Backend.slnx

# 发布发布版本
dotnet publish -c Release Services/User.Service
```

### 7.2 前端编译

| 目标 | 命令 | 说明 |
|------|------|------|
| 管理端 Web | `npm run build` | 部署到Nginx |
| 管理端 H5 | `npm run build:h5` | 移动端HTML |
| 管理端 Android | `npm run build:android` | Capacitor APK |
| 商家端 Web | `npm run build` | 部署到Nginx |
| 商家端 H5 | `npm run build:h5` | 移动端HTML |
| 商家端 Android | `npm run build:android` | Capacitor APK |
| 移动端 H5 | `npm run build:h5` | UniApp H5 |
| 微信小程序 | `npm run build:mp-weixin` | 微信开发者工具导入 |

### 7.3 Docker 完整部署

```bash
# 1. 启动基础设施
docker-compose up -d

# 2. 构建各服务镜像
docker build -f Services/User.Service/Dockerfile -t campus-user .
# ... 其他7个服务同理

# 3. 完整编排（生产环境）
docker-compose -f docker-compose.yml -f docker-compose.services.yml up -d
```

## 8. 常见问题

### Q1: "dotnet build" 报错 ?
确保安装了 .NET 10.0 SDK。

### Q2: 数据库连接失败 ?
检查 `appsettings.json` 中的连接字符串，确保 MySQL Docker容器已启动。

### Q3: UniApp 编译报 "uni 不是内部命令" ?
运行 `npm install --legacy-peer-deps` 安装依赖。

### Q4: Android APK 打包失败 ?
需要安装 Android Studio 和 Android SDK，确保 JAVA_HOME 和 ANDROID_HOME 环境变量配置正确。

### Q5: 端口被占用 ?
修改对应服务的 `appsettings.json` 或 `Program.cs` 中的端口号。注意同步更新 ocelot.json 中的下游端口。